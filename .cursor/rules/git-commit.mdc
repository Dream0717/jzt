---
description: 每次代码修改完成后自动 git commit，提交信息需说明改动内容
alwaysApply: true
---

# Git 提交规范

完成代码修改后，**必须**创建 git commit，不得遗留未提交改动（除非用户明确说不要提交）。

## 提交时机

- 完成一项功能、修复或重构后立即提交
- 一次对话中若有多项独立改动，可分多次提交，每次对应一类改动

## 提交信息格式

使用中文，1-2 句话，说明「改了什么」和「为什么」：

```
<类型>: <简要说明>

<可选：补充细节>
```

类型示例：`feat` 新功能、`fix` 修复、`refactor` 重构、`style` 样式、`docs` 文档

## 安全

- 绝不提交 `.env`、密钥、凭证
- 不 force push 到 main
- 仅在用户要求时 push 到远程

## 流程

1. `git status` + `git diff` 确认改动
2. `git add` 相关文件
3. `git commit -m "..."` 写清楚改动内容
4. 若远程已配置且用户要求同步，则 `git push`
